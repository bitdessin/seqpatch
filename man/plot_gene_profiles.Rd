% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_gene_profiles}
\alias{plot_gene_profiles}
\title{Plot Gene Expression Profiles}
\usage{
plot_gene_profiles(
  x,
  cl,
  by = "group",
  zscale = TRUE,
  log_transform = TRUE,
  pseudocount = 1,
  ncol = NULL,
  line_color = "#7A7A7A",
  line_alpha = 0.15,
  line_width = 0.35,
  mean_color = "#202020",
  mean_width = 1.1
)
}
\arguments{
\item{x}{A \code{SeqCountData} object with normalization factors stored in
\code{x@meta$nf}.}

\item{cl}{Integer or character vector assigning each gene to a group or
cluster. Its length must equal the number of genes in \code{x}.}

\item{by}{Column name in \code{x@exp_design} used to average biological
replicates before plotting. Defaults to \code{"group"}. If \code{NULL}, every sample
is plotted separately.}

\item{zscale}{Logical value indicating whether expression values should be
converted to gene-wise z-scores.}

\item{log_transform}{Logical value indicating whether normalized counts
should be transformed with \code{log2(value + pseudocount)}.}

\item{pseudocount}{Non-negative value added before log transformation.}

\item{ncol}{Number of columns used to arrange the gene-group facets.}

\item{line_color}{Color used for individual gene profiles.}

\item{line_alpha}{Opacity used for individual gene profiles.}

\item{line_width}{Width used for individual gene profiles.}

\item{mean_color}{Color used for gene-group mean profiles.}

\item{mean_width}{Width used for gene-group mean profiles.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots normalized expression profiles for genes grouped by a cluster or other
label. Individual gene profiles are shown as light lines, and the mean
profile for each gene group is overlaid as a darker line.
}
\details{
Normalized counts are obtained with \code{\link[=norm_counts]{norm_counts()}}. When \code{by} is specified,
normalized counts are averaged across samples with the same value in the
corresponding column of \code{x@exp_design}. The values are then optionally log
transformed and scaled to z-scores within each gene.
}
\examples{
set.seed(1)
x <- sim_gene_ts_counts(2000)
cl <- x@meta$ts_truth$pattern

x <- calc_nf(x)
plot_gene_profiles(x, cl)

}
