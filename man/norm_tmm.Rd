% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/norm_funcs.R
\name{norm_tmm}
\alias{norm_tmm}
\title{Estimate TMM Normalization Factors}
\usage{
norm_tmm(x, exp_design, ...)
}
\arguments{
\item{x}{A count matrix.}

\item{exp_design}{Sample metadata as a data frame. Must contain \code{group}.}

\item{...}{Additional arguments passed to \code{edgeR::calcNormFactors()}.}
}
\value{
Numeric normalization factors, one per sample.
}
\description{
Estimate TMM Normalization Factors
}
\examples{
if (requireNamespace("edgeR", quietly = TRUE)) {
    set.seed(1)
    counts <- matrix(stats::rnbinom(600, mu = 50, size = 5), nrow = 100)
    colnames(counts) <- paste0("sample_", seq_len(ncol(counts)))
    rownames(counts) <- paste0("gene_", seq_len(nrow(counts)))
    exp_design <- data.frame(group = rep(c("A", "B"), each = 3))
    x <- newSeqCountData(counts, exp_design = exp_design)
    x <- calc_nf(x, norm_func = norm_tmm, test_func = test_edger, iter = 1)
    x@meta$nf
}

}
