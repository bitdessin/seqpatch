% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/norm.R
\name{calc_nf}
\alias{calc_nf}
\title{Estimate Normalization Factors}
\usage{
calc_nf(
  x,
  norm_func = norm_tmm,
  test_func = test_edger,
  iter = 3,
  p_cutoff = 0.05,
  q_cutoff = 0.1,
  min_rm_genes = 0.05,
  ...
)
}
\arguments{
\item{x}{A \code{SeqCountData} object.}

\item{norm_func}{Function used to estimate normalization factors. It should
accept a count matrix \code{x}, sample metadata \code{exp_design}, and optional
method-specific arguments.}

\item{test_func}{Function used for DEG testing. It should
accept a count matrix \code{x}, sample metadata \code{exp_design}, normalization
factors \code{nf}, and optional method-specific arguments.}

\item{iter}{Number of DEG elimination iterations after the initial
normalization.}

\item{p_cutoff}{p-value cutoff for removing potential DEGs. Use \code{NULL}
to ignore p-value filtering.}

\item{q_cutoff}{Adjusted p-value cutoff for removing potential DEGs. Use
\code{NULL} to ignore adjusted p-value filtering.}

\item{min_rm_genes}{Minimum proportion of genes to remove at each iteration.}

\item{...}{Additional arguments passed to \code{norm_func} and \code{test_func} when
their signatures accept them.}
}
\value{
A \code{SeqCountData} object with updated normalization factors. DEGES
settings and potential DEG indicators are stored in \code{x@meta$DEGES}.
}
\description{
Performs a compact DEGES workflow:
}
\details{
\enumerate{
\item Estimate normalization factors with \code{norm_func}.
\item Screen candidate DEGs with \code{test_func}.
\item Remove genes passing \code{p_cutoff} or \code{q_cutoff}. If fewer than
\code{min_rm_genes} of genes would be removed, remove the top
\code{min_rm_genes} proportion ranked by p-value.
\item Estimate normalization factors again from the remaining genes.
}

Steps 2 through 4 are repeated \code{iter} times.
}
\examples{
if (requireNamespace("edgeR", quietly = TRUE)) {
    set.seed(1)
    counts <- matrix(stats::rnbinom(600, mu = 50, size = 5), nrow = 100)
    colnames(counts) <- paste0("sample_", seq_len(ncol(counts)))
    rownames(counts) <- paste0("gene_", seq_len(nrow(counts)))
    exp_design <- data.frame(group = rep(c("A", "B"), each = 3))
    x <- newSeqCountData(counts, exp_design = exp_design)
    x <- calc_nf(x, iter = 1)
    x@meta$nf
}

}
