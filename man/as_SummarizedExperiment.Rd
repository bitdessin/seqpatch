% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SeqCountData.R
\name{as_SummarizedExperiment}
\alias{as_SummarizedExperiment}
\title{Convert to a SummarizedExperiment}
\usage{
as_SummarizedExperiment(x)
}
\arguments{
\item{x}{A \code{SeqCountData} object.}
}
\value{
A \code{SummarizedExperiment} object with counts stored in the \code{counts}
assay, \code{x@exp_design} stored as \code{colData}, gene names stored as \code{rowData},
and \code{x@meta} stored as object metadata.
}
\description{
Convert to a SummarizedExperiment
}
\examples{
if (requireNamespace("SummarizedExperiment", quietly = TRUE)) {
    counts <- matrix(1:12, nrow = 3)
    colnames(counts) <- paste0("sample_", seq_len(ncol(counts)))
    rownames(counts) <- paste0("gene_", seq_len(nrow(counts)))
    exp_design <- data.frame(group = rep(c("A", "B"), each = 2))
    x <- newSeqCountData(counts, exp_design = exp_design)
    se <- as_SummarizedExperiment(x)
    SummarizedExperiment::assayNames(se)
}

}
