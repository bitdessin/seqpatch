% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_ma}
\alias{plot_ma}
\title{Plot an MA Plot}
\usage{
plot_ma(x, comparison = NULL, esp = 1, col = NULL)
}
\arguments{
\item{x}{A \code{SeqCountData} object.}

\item{comparison}{Optional vector of two group names to compare. If
\code{NULL}, the first two groups in \code{x@exp_design$group} are used.}

\item{esp}{Value added before log transformation.}

\item{col}{Optional point colors. Can be a single color or one color per gene.
When provided, points are drawn from the most frequent color category to
the least frequent category.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates an MA plot from normalized counts for two groups in \code{x@exp_design$group}.
Group averages are computed before log transformation for the A values.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
    set.seed(1)
    counts <- matrix(stats::rnbinom(600, mu = 50, size = 5), nrow = 100)
    colnames(counts) <- paste0("sample_", seq_len(ncol(counts)))
    rownames(counts) <- paste0("gene_", seq_len(nrow(counts)))
    exp_design <- data.frame(group = rep(c("A", "B"), each = 3))
    x <- newSeqCountData(
        counts,
        exp_design = exp_design,
        norm_factors = rep(1, ncol(counts))
    )
    plot_ma(x)
}

}
