% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_funcs.R
\name{test_deseq2}
\alias{test_deseq2}
\title{Run DESeq2 to Detect DEGs}
\usage{
test_deseq2(
  x,
  exp_design,
  nf = NULL,
  design = NULL,
  full = NULL,
  reduced = NULL,
  contrast = NULL,
  ...
)
}
\arguments{
\item{x}{A count matrix.}

\item{exp_design}{A data frame of experimental design. Must contain \code{group}.}

\item{nf}{A vector of normalization factors.}

\item{design}{Optional DESeq2 design formula. Defaults to \code{~ group}.}

\item{full}{Optional full model for an LRT. Defaults to
\code{design}.}

\item{reduced}{Optional reduced model for an LRT. Defaults
to \code{~ 1}.}

\item{contrast}{Optional contrast passed to \code{DESeq2::results()}.}

\item{...}{Reserved for future extension.}
}
\value{
A data frame with at least \code{p_value} and \code{q_value}
columns.
}
\description{
Runs a DESeq2 LRT to detect DEGs. By default, this function tests
for differences across the groups
defined by the \code{group} column of \code{exp_design}.
}
\examples{
if (requireNamespace("DESeq2", quietly = TRUE)) {
    set.seed(1)
    counts <- matrix(stats::rnbinom(600, mu = 50, size = 5), nrow = 100)
    colnames(counts) <- paste0("sample_", seq_len(ncol(counts)))
    rownames(counts) <- paste0("gene_", seq_len(nrow(counts)))
    exp_design <- data.frame(group = rep(c("A", "B"), each = 3))
    x <- newSeqCountData(counts, exp_design = exp_design)
    x <- calc_nf(x, norm_func = norm_rle, test_func = test_deseq2, iter = 1)
    x@meta$DEGES$params
}

}
