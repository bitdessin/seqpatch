% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SeqCountData.R
\name{as_DESeqDataSet}
\alias{as_DESeqDataSet}
\title{Convert to a DESeq2 DESeqDataSet}
\usage{
as_DESeqDataSet(x, design = ~group)
}
\arguments{
\item{x}{A \code{SeqCountData} object.}

\item{design}{A DESeq2 design formula. Defaults to \code{~ group}.}
}
\value{
A DESeq2 \code{DESeqDataSet}.
}
\description{
Convert to a DESeq2 DESeqDataSet
}
\examples{
if (requireNamespace("DESeq2", quietly = TRUE)) {
    counts <- matrix(1:12, nrow = 3)
    colnames(counts) <- paste0("sample_", seq_len(ncol(counts)))
    rownames(counts) <- paste0("gene_", seq_len(nrow(counts)))
    exp_design <- data.frame(group = rep(c("A", "B"), each = 2))
    x <- newSeqCountData(
        counts,
        exp_design = exp_design,
        norm_factors = rep(1, ncol(counts))
    )
    dds <- as_DESeqDataSet(x)
    DESeq2::design(dds)
}

}
