% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_gene_ts_counts}
\alias{sim_gene_ts_counts}
\title{Simulate Time-series RNA-seq Gene Counts}
\usage{
sim_gene_ts_counts(
  n_genes = 10000,
  ts = 0:7,
  n_replicates = 3,
  p_patterns = NULL,
  fc_mean = 4,
  fc_dist = c("lognormal", "gamma", "fixed"),
  fc_sd = 0.3,
  cv_libsize = 0.2,
  seed_counts = NULL
)
}
\arguments{
\item{n_genes}{Number of genes to simulate.}

\item{ts}{Numeric vector of ordered sampling times.}

\item{n_replicates}{Number of biological replicates per time point. Either a
single value or a vector with one value per time point.}

\item{p_patterns}{Relative proportions of the eight temporal patterns.
If \code{NULL}, all patterns are equally represented. An unnamed numeric vector
must have length eight and follows the pattern order given above. A named
vector may specify any subset of the eight pattern names; omitted patterns
receive probability zero. Values are normalized to sum to one.}

\item{fc_mean}{Mean dynamic-range fold change for non-null genes. The
generated temporal profile is scaled so that, before count sampling, the
ratio between its largest and smallest expected fold-change multipliers is
the gene-specific value sampled from \code{fc_dist}.}

\item{fc_dist}{Distribution used to generate gene-specific dynamic-range fold
changes. One of \code{"lognormal"}, \code{"gamma"}, or \code{"fixed"}.}

\item{fc_sd}{Spread parameter for randomized dynamic-range fold changes. Its
interpretation is the same as in \code{\link[=sim_gene_counts]{sim_gene_counts()}}.}

\item{cv_libsize}{Coefficient of variation of sample-specific
library-size factors. Factors are drawn from a mean-one log-normal
distribution and rescaled to have arithmetic mean one. Set to 0 to disable
library-size variation.}

\item{seed_counts}{Optional seed count matrix. Rows are genes and columns are
samples. If \code{NULL}, the package dataset \code{arab} is used.}
}
\value{
A \linkS4class{SeqCountData} object. The \code{meta} slot contains
\code{sim_params}, \code{libsize_factors}, and \code{ts_truth}. The \code{ts_truth} entry
stores the true temporal pattern assigned to each gene, requested and
realized pattern proportions, temporal pattern templates, gene-specific
dynamic-range fold changes, and sampling times.
}
\description{
Generates synthetic time-series RNA-seq counts with known latent temporal
patterns. Gene-specific baseline abundance and negative-binomial dispersion
are sampled from the same seed-derived population used by
\code{\link[=sim_gene_counts]{sim_gene_counts()}}. Each simulated gene is assigned to one of eight latent
temporal patterns, and the expected expression trajectory is generated as a
fold-change profile around the sampled baseline abundance. Sample-specific
library-size variation can be added independently of the temporal pattern.
}
\details{
The eight built-in patterns are \code{"increase"}, \code{"decrease"}, \code{"early_peak"},
\code{"late_peak"}, \code{"biphasic"}, \code{"oscillatory"}, \code{"delayed_increase"}, and
\code{"null"}. Pattern functions are evaluated on the supplied numeric sampling
times after scaling them to the interval from 0 to 1, so unevenly spaced
time points are represented explicitly.
}
\examples{
x <- sim_gene_ts_counts()
table(x@meta$ts_truth$pattern)

}
