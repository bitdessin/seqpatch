% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_funcs.R
\name{test_edger}
\alias{test_edger}
\title{Run edgeR to Detect DEGs}
\usage{
test_edger(
  x,
  exp_design,
  nf = NULL,
  design = NULL,
  coef = NULL,
  contrast = NULL,
  ...
)
}
\arguments{
\item{x}{A count matrix.}

\item{exp_design}{A data frame of experimental design. Must contain \code{group}.}

\item{nf}{A vector of normalization factors.}

\item{design}{Optional model matrix. Defaults to \code{model.matrix(~ group, data = exp_design)}.}

\item{coef}{Coefficient or coefficients passed to \code{edgeR::glmQLFTest()}.}

\item{contrast}{Optional contrast passed to \code{edgeR::glmQLFTest()}.}

\item{...}{Reserved for future extension.}
}
\value{
A data frame with \code{p_value} and \code{q_value} columns.
}
\description{
Runs quasi-likelihood F tests to detect DEGs
using edgeR. By default, this function tests for differences across the
groups defined by the \code{group} column of \code{exp_design}.
}
\examples{
if (requireNamespace("edgeR", quietly = TRUE)) {
    set.seed(1)
    counts <- matrix(stats::rnbinom(600, mu = 50, size = 5), nrow = 100)
    colnames(counts) <- paste0("sample_", seq_len(ncol(counts)))
    rownames(counts) <- paste0("gene_", seq_len(nrow(counts)))
    exp_design <- data.frame(group = rep(c("A", "B"), each = 3))
    x <- newSeqCountData(counts, exp_design = exp_design)
    x <- calc_nf(x, norm_func = norm_tmm, test_func = test_edger, iter = 1)
    x@meta$DEGES$params
}

}
