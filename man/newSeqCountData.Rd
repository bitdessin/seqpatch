% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SeqCountData.R
\name{newSeqCountData}
\alias{newSeqCountData}
\title{Create a SeqCountData Object}
\usage{
newSeqCountData(
  data,
  gene_names = NULL,
  exp_design = NULL,
  meta = list(),
  norm_factors = NULL
)
}
\arguments{
\item{data}{Matrix object, edgeR \code{DGEList}, DESeq2 \code{DESeqDataSet}, or
\code{SummarizedExperiment}. For matrix input, rows are genes and columns are
samples.}

\item{gene_names}{Optional vector of gene names. Defaults to row names of
\code{data} when present.}

\item{exp_design}{Data frame describing sample groupings. For \code{DGEList},
\code{DESeqDataSet}, and \code{SummarizedExperiment} input, this defaults to the
sample metadata stored in the input object. It must include a column named
\code{group}.}

\item{meta}{Optional list containing additional metadata.}

\item{norm_factors}{Optional numeric normalization factors. If provided, they
are stored as \code{meta$nf}. For \code{DGEList} input, this defaults to
\code{data$samples$norm.factors} when available. For \code{DESeqDataSet} input, this
is derived from \code{DESeq2::sizeFactors(data)} when available.}
}
\value{
A \code{SeqCountData} object.
}
\description{
Create a SeqCountData Object
}
\examples{
counts <- matrix(1:12, nrow = 3)
colnames(counts) <- paste0("sample_", seq_len(ncol(counts)))
rownames(counts) <- paste0("gene_", seq_len(nrow(counts)))
exp_design <- data.frame(group = rep(c("A", "B"), each = 2))
x <- newSeqCountData(counts, exp_design = exp_design)
x

}
